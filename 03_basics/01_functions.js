function calculateCartPrice(...num){
    return num
}

// console.log(calculateCartPrice(695,700,800));

const user = {
    username: "Priyanshu",
    password: "5864123"
}

function handleObject(anyobject) {
    console.log(`Username is ${anyobject.username} and password is ${anyobject.password}`);
}

handleObject(user)
handleObject({
    username: "ArrowOG",
    password: "7879515"
})